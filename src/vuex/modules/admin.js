const state = {
    newMatch:[],//新增需求匹配
}
const mutations = {
    ADDNEWMATCH(state,{newMatch}){
        state.newMatch=newMatch
    }

}
const actions = {

}
const getters = {
 
}

export default {
  state,
  mutations,
  actions,
  getters
}